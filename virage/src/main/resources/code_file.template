#include "types.h"
#include "modules.h"
#include "aggregators.h"
#include "termination_conditions.h"

$CONTENT

result voting_rule(profile p) {
  result r;
  for(int i=0; i<C; i++) {
    r.values[i] = DEFERRED;
  }

  r = $ENTRY;

  return r;
}
