
(*
 *  cd ..../theories
 *
 *  isabelle build -o quick_and_dirty -b -D `pwd`
 *    # compule sessions in the ROOT file to prebuilt images
 *    # quick and dirty needed due to the "sorry" command somewhere
 *
 *  isabelle jedit -d `pwd` -l VOTING
 *    # use an image for interactive mode; probably similar for servermode.
 *
 * Theories can then be accessed as "VOTING.linear_consistency" eg.
 *)


session VOTING = HOL +
  options [document = pdf, document_output = "output"]
  theories
    electoral_modules
    condorcet_consistency
    linear_orders
    (* ... *)
  document_files
    "root.tex"
